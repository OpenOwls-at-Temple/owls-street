{
  "name": "owls-street-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd owl-street-view/frontend && npm install && npm run build && cd ../.. && mkdir -p build && cp -r owl-street-view/frontend/build/* build/"
  }
}
