node_dist/
*.log
.venv/
__pycache__/
.pytest_cache/
.DS_Store
owl-street-view/logs/
owl-street-view/frontend/build/
owl-street-view/frontend/node_modules/

# Secrets and credential exclusions
.env
.env.*
google-oauth-secret.json

# Vercel CLI link state — contains project and org ids, and is per-developer
.vercel/

# Local databases state
*.db
data/
.vercel
.env*
# ...but the fill-in-the-blanks template holds no secrets and is meant to be shared.
!.env.vercel.example

# Generated by `vercel build` from each app's requirements.txt, pinned to whatever Python
# the local machine has. Committing them would pin Vercel's build to it too.
pyproject.toml
uv.lock
