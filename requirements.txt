# Dependencies for the combined single-project Vercel deployment.
#
# Vercel installs one requirements.txt from the project root for every Python function, so
# this is the union of owls-street-view/requirements.txt and
# owls-street-pulse/requirements.txt. Those files remain the source of truth for local
# development, Docker, and CI — keep this in sync when either changes.
#
# pytest is deliberately absent: nothing runs tests inside a function, and both bundles are
# already close enough to Vercel's size limit that the dependency is not worth carrying.
requests>=2.31.0
pandas>=2.0.0
pyyaml>=6.0.1
pydantic>=2.0.0
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
alpaca-py>=0.26.0
pytz
websockets>=12.0
httpx>=0.24.0
google-auth>=2.0.0
python-multipart>=0.0.5
